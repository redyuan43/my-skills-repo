interface:
  display_name: "Codex Proxy Env Fix"
  short_description: "修复 Codex 代理与 WebSocket 连接"
  default_prompt: "Use $codex-proxy-env-fix to diagnose Codex reconnecting or MCP proxy failures and write a correct ~/.codex/.env."
