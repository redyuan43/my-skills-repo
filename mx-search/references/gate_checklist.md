# Gate Checklist

- 只做金融资讯、公告、研报、政策和事件检索，不替代行情或财务表格查询。
- 请求前确认 `MX_APIKEY` 来自环境变量，不落盘真实密钥。
- 依赖安装优先 venv，不默认建议 `--break-system-packages`。
- 输出保留 `.txt` 可读结果和 `.json` 原始响应。
- 对时效性问题优先说明来源和日期，不把旧资讯当最新事实。
