# Gate Checklist

- 只处理金融数据查询，不做交易建议、不改自选股、不改模拟组合。
- 请求前确认 `MX_APIKEY` 来自环境变量或受信任配置，不写入文档和输出文件。
- 默认输出目录必须是 `~/.mx/mx-data/output/`，除非用户显式传入输出目录。
- 大范围历史数据先缩小查询条件，避免超大响应占满上下文。
- API 返回空表时先调整自然语言查询，不把空结果直接解释成公司不存在或数据为零。
