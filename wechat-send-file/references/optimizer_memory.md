# Optimizer Memory

- Safety gate: target, file/auto-pick rule, and send intent must all be explicit.
- Bot route uses WeClaw context; normal route uses PyWxDump GUI pipeline.
- Safe validation uses `--print-only` or `scripts/selftest.sh --safe`; no live send in eval.
