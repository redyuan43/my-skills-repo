# Gate Checklist

- 查询自选股可直接运行。
- `add`、`delete` 和自然语言管理动作属于状态变更，默认拒绝真实调用。
- 状态变更必须显式传 `--yes`；`--dry-run` 只输出 payload，不请求接口。
- dry-run 不要求 `MX_APIKEY`，真实查询或执行才读取密钥。
- 不把筛选结果批量加入或删除自选股，除非用户明确要求并逐步确认。
