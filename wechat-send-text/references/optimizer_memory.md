# Optimizer Memory

- Safety gate: live send requires explicit target, explicit text, and explicit send intent.
- Validation should prefer `--print-only` and `scripts/selftest.sh --safe`.
- Keep PyWxDump venv guidance intact: use `~/github/PyWxDump/.venv/bin/python` for live sends on this machine.
